import RoomStore from '../../../src/conf/store/room';

let room;
beforeEach(() => {
  room = new RoomStore();
});

describe('a', () => {
  it('a', () => {
    room;
    console.log(MediaStream);
    console.log(room.localStream);
  });
});
